data_gen:
  API_KEY : "AIzaSyB4PI14X_caKdryrTukPztiogrBBeSb_9A"
  TYPE : "incomplete"  # Choose either "ambiguous", "inconsistent" or "incomplete"
  venv : "./venv/Scripts/python.exe"
  
  # Base directories - everything will be organized under these
  build_dir: "build"
  output_dir: "outputs"
  input_dir: "input/APPS/train"
  
  # File naming patterns
  filenames:
    step2_output: "modified_{type}_train.jsonl"
    step3_output: "clarity_{type}_train.jsonl" 
    final_output: "finetuning_{type}_data.jsonl"
    step2_checkpoint: "modified_checkpoint.txt"
    step3_checkpoint: "clarify_checkpoint.txt"